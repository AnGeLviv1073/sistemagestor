# Importamos la función 'crear_login' desde el módulo 'login_window'
from login_window import crear_login
# Verificamos si este archivo se está ejecutando directamente (no importado como módulo)
if __name__ == "__main__":
    crear_login()
